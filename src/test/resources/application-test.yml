spring:
  mail:
    host: localhost
    port: 25564
    defaultEncoding: UTF-8
    protocol: smtp
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://test.gov/test
    
security:
  basic:
    enabled: false
  require-ssl: false

mlr:
  email:
    template: 
      from: test@test.net